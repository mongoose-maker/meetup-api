export class Meetup {
    constructor(id, title, description, tags, date, location) {
        this.id = id,
        this.title = title,
        this.description = description,
        this.tags = tags,
        this.date = date,
        this.location = location
    };
};